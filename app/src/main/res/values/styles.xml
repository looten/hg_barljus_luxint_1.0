<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="AppThemeLuxint">

        <!-- Customize your theme here. -->

        <item name="fontPath">fonts/gnuolanerg.ttf</item>
        <item name="android:textColor">#000000</item>

       <item name="android:windowBackground">@color/default_background</item>
        <!--
          Theme.AppCompat.Light
               <item name="android:windowBackground">@color/appthemeluxint_btn_default_holo_light</item>

               <item name="android:colorBackground">@color/background_material_light</item>
               -->

    </style>

    <style name="Theme.Light.AppTheme" parent="android:Theme.Light" />

    <style name="TEST" parent="Theme.AppCompat.Light.NoActionBar" />


</resources>
